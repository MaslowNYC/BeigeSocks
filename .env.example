# Supabase project that backs the shared Pack list (project: "gabber").
# The publishable key is safe to ship to the browser: RLS is on with no policies,
# so all access goes through the pack_* SECURITY DEFINER functions, which require a trip token.
VITE_SUPABASE_URL=https://orqqcayhyjlmrdfaakup.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_...
